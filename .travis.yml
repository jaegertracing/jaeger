sudo: required

language: go

go:
  - 1.7

services:
  - docker

#before_install:
#  - sudo apt-get -qq update
#  - sudo apt-get install -y jq-1.5
#  - jq --version

install:
  - make install_ci

script:
  - make test_ci

