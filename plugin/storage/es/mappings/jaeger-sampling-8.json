{
  "priority": {{ .Indices.Sampling.TemplateOptions.Priority }},
  "index_patterns": "{{ .IndexPrefix }}jaeger-sampling-*",
  "template": {
    {{- if .UseILM }}
    "aliases": {
      "{{ .IndexPrefix }}jaeger-sampling-read": {}
    },
    {{- end }}
    "settings": {
      "index.number_of_shards": {{ .Indices.Sampling.TemplateOptions.NumShards }},
      "index.number_of_replicas": {{ .Indices.Sampling.TemplateOptions.NumReplicas }},
      "index.mapping.nested_fields.limit": 50,
      "index.requests.cache.enable": false
      {{- if .UseILM }},
      "lifecycle": {
        "name": "{{ .ILMPolicyName }}",
        "rollover_alias": "{{ .IndexPrefix }}jaeger-sampling-write"
      }
      {{- end }}
    },
    "mappings": {}
  }
}
