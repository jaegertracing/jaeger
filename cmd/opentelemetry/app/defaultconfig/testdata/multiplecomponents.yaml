receivers:
  jaeger:
    protocols:
      grpc:
      thrift_compact:
      thrift_binary:

processors:

exporters:
  jaeger_elasticsearch:
    server_urls: whatever

service:
  pipelines:
    traces:
      receivers: [jaeger]
      processors: []
      exporters: [jaeger_elasticsearch]
