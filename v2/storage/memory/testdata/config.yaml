receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  memory:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [memory]
