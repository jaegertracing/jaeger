name: Debug Workflow Visibility

on:
  workflow_dispatch:

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Hello! If you see this workflow, the repository is working."