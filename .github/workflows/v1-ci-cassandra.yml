name: v1 - CIT Cassandra

on:
  push:
    branches: [ master ]
    paths-ignore:
    - v2/**
  pull_request:
    branches: [ master ]
    paths-ignore:
    - v2/**

jobs:
  cassandra:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-go@v2
      with:
        go-version: ^1.16

    - name: Run cassandra integration tests
      run: bash scripts/cassandra-integration-test.sh
